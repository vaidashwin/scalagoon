(ns scalabotlib.ircmodule)
(gen-class
  :name scalabotlib.testmodule.IrcModuleClj
  :methods [[func [String, String, String] String]
            [regex [] String]])

(defn func [_, user, channel, message] "")

(defn regex [_] "")

(def -helpBlurb "")